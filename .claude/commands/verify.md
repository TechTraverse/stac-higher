Run the full verification suite for the project. Execute these steps in order and report results:

1. Type check: `cd $CLAUDE_PROJECT_DIR/app && npx astro check`
2. Build: `cd $CLAUDE_PROJECT_DIR/app && npx astro build`
3. Unit tests: `cd $CLAUDE_PROJECT_DIR/app && npm test`

Report the result of each step. If any step fails, stop and report the failure with details.
